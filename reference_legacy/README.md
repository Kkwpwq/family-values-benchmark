# Frozen legacy comparison tables

These Methods-V2 tables are retained only because the packaged recalculation scripts explicitly compare the current Methods-3.5-aligned estimates with the archived estimates. They are reference inputs for reproducibility audits, not the final inferential tables reported by the current release.
